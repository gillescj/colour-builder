import React from 'react';
import NumberSelector from './NumberSelector';

const App = () => {
    return (
        <div className="container">
            <NumberSelector />
        </div>
    );
};

export default App;
